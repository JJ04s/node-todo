const command = process.argv[2];
const taskOrId = process.argv[3];

switch(command){
    case "add":
        //addToDo 함수 실행
        break;
    
    case "list":
        //listToDos 함수 실행
        break;

    case "done":
        //doneToDo 함수 실행
        break;

    case "delete":
        // delteToDo 함수 실행
        break;

    case "update":
        // updateToDo 함수 실행
        break;

}